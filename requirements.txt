# Edge VLM Thesis — Dependencies
# Install: pip install -r requirements.txt
#
# Kaggle T4 environment uses Python 3.10+

# ── Core Vision ────────────────────────────────────────────
torch>=2.1.0
torchvision>=0.16.0
opencv-python>=4.8.0
Pillow>=10.0.0
numpy>=1.24.0

# ── Detection ──────────────────────────────────────────────
ultralytics>=8.0.0          # YOLOv8 nano

# ── Tracking ───────────────────────────────────────────────
# NOTE: boxmot v10-v18 have ByteTrack; v19+ changed API
# For modern Python 3.12: use boxmot 10.x with PyTorch
# Install separately to avoid dependency conflicts:
# pip install boxmot==10.11.62 (last stable v10 release)
# If using modern boxmot (v21+), update bytetrack_wrapper.py accordingly

# ── Embeddings ─────────────────────────────────────────────
# OpenAI CLIP — install via:
# pip install git+https://github.com/openai/CLIP.git
# (included below as fallback)
ftfy>=6.1.0
regex>=2023.0.0

# ── VLM ────────────────────────────────────────────────────
transformers>=4.45.0        # Qwen2.5-VL requires >=4.45; Moondream needs >=4.40
accelerate>=0.28.0
einops>=0.7.0
qwen-vl-utils>=0.0.8        # Qwen2.5-VL image processing utilities

# Optional: 4-bit quantization (Qwen 7B on single T4 ~9GB instead of ~14GB)
bitsandbytes>=0.43.0

# ── Experiment Tooling ─────────────────────────────────────
pyyaml>=6.0
tqdm>=4.66.0
pandas>=2.0.0

# ── Evaluation & Metrics ───────────────────────────────────
scikit-learn>=1.3.0
scipy>=1.11.0

# ── Visualization ──────────────────────────────────────────
matplotlib>=3.8.0
seaborn>=0.13.0
plotly>=5.18.0              # interactive Pareto plots

# ── Notebook ───────────────────────────────────────────────
jupyterlab>=4.0.0
ipywidgets>=8.0.0

# ── Utilities ──────────────────────────────────────────────
psutil>=5.9.0               # CPU/memory monitoring
gputil>=1.4.0               # GPU utilization logging

# ── Optional: ONNX export ──────────────────────────────────
# onnx>=1.15.0
# onnxruntime-gpu>=1.16.0
